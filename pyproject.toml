[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py310"
line-length = 100
select = ["E", "F", "W", "I"]

[tool.ruff.isort]
known-first-party = ["app", "web", "untatiz", "statiz_utils"]
